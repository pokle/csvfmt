# Development
- `bun install` to install dependencies
- `bun link` to install it on your computer.
- `bun link` here, then `bun link csvfmt` from anywhere to install the dev version globally
- `bun run test` (or `./test.sh`) to run the smoke tests
- Bump the version and release with `npm publish` (the package is published to npmjs)
